# Database Configuration
DB_URL=jdbc:mysql://localhost:3306/tuition
DB_USERNAME=tuition_user
DB_PASSWORD=your_secure_password_here

# Application Settings
SERVER_PORT=8080
SHOW_SQL=false
DDL_AUTO=validate

# Production Settings
# DDL_AUTO=validate (never use 'update' or 'create-drop' in production)
# SHOW_SQL=false (never show SQL in production logs)